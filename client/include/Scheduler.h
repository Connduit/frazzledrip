/* Scheduler.h */
/*
 * decides when and the order in which messages are sent to the server?
 * some messsages should be sent instantly, some should be sent based on 
 * their priority, and some should be delayed before being sent to delay
 * susipicion
 * */

// takes in messageConsumer_ (so transportlayer or messageHandler, probs transportlayer)
